import { ActionTree } from 'vuex'
import { CalibrationState } from '@/store/calibration/types'
import { RootState } from '@/store/types'

export const actions: ActionTree<CalibrationState, RootState> = {}
